server {
  listen 5000;
  listen [::]:5000;
  server_name dvs, gpu09-dt, planet31, localhost;

  location / {
    root /home/arthurf/dvs/slamvizapp/slamvizapp-webapp/build;
    try_files $uri $uri/index.html $uri.html /index.html;
  }

  location ^~ /api {
      include uwsgi_params;
      uwsgi_pass unix:/home/arthurf/dvs/slamvizapp/deployment/slamvizapp.sock;
      uwsgi_read_timeout 300;
  }

  location ^~ /webhook {
      include uwsgi_params;
      uwsgi_pass unix:/home/arthurf/dvs/slamvizapp/deployment/slamvizapp.sock;
  }

 location ^~ /s/ {
  alias /home/arthurf/ci/dvs/psp_swip/;
  access_log off;

 }

 location ^~ /ss/ {
  alias /net/f2/algo_archive/PTAM_Results/; 
  access_log off;
 }

}


server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  server_name dvs,planet31,gpu09-dt,localhost;

  # ssl_dhparam  /etc/nginx/ssl/dhparam.pem;
  ssl_certificate /etc/nginx/ssl/gpu09-dt.crt;
  ssl_certificate_key /etc/nginx/ssl/gpu09-dt.key;

  location / {
    root /home/arthurf/dvs/slamvizapp/slamvizapp-webapp/build;
    try_files $uri $uri/index.html $uri.html /index.html;
  }

  location ^~ /api {
      include uwsgi_params;
      uwsgi_pass unix:/home/arthurf/dvs/slamvizapp/deployment/slamvizapp.sock;
  }

 location ^~ /s/ {
  alias /home/arthurf/ci/dvs/psp_swip/;
  access_log off;

 }

 location ^~ /ss/ {
  alias /net/f2/algo_archive/PTAM_Results/; 
  access_log off;
 }
 
}

server {
   listen         80;
   listen    [::]:80;
   server_name    dvs,planet31,gpu09-dt;
   return         301 https://$server_name$request_uri;
}
