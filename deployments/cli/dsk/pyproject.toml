[project]
name = "qaboard-site-dsk"
version = "1.0.0"
description = "QABoard site defaults for Samsung DSK"
requires-python = ">=3.7"

[project.entry-points."qaboard.site"]
dsk = "qaboard_site_dsk:defaults"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
